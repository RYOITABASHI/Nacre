*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/ime-core/build
/ime-config/build
/ime-ai/build
/captures
.externalNativeBuild
.cxx
local.properties
*.apk
*.aab
# Raw Mozc dictionary files (used for building, not shipped)
tools/mozc_raw/
# KenLM vendored source (too large for git, cloned locally)
ime-ai/src/main/cpp/kenlm/
# KenLM cloned source
/kenlm-src/
# Signing keys — NEVER commit
*.keystore
*.jks
signing.properties

# Model staging (large files, not for repo)
models-staging/

# Extracted AAR artifacts
META-INF/
.worktrees/
